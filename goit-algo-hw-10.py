##### ДЗ. Тема 10. Лінійне програмування та рандомізовані алгоритми

### Завдання 1. Оптимізація виробництва

'''
Компанія виробляє два види напоїв: "Лимонад" і "Фруктовий сік". Для виробництва цих напоїв використовуються різні інгредієнти та обмежена кількість обладнання. Задача полягає у максимізації виробництва, враховуючи обмежені ресурси.

Умови завдання:

1. "Лимонад" (A) виготовляється з "Води" (x), "Цукру" (y) та "Лимонного соку"(z).
2. "Фруктовий сік" (B) виготовляється з "Фруктового пюре" (s) та "Води"(x).

3. Обмеження ресурсів: 100 од. "Води", 50 од. "Цукру", 30 од. "Лимонного соку" та 40 од. "Фруктового пюре".

4. Виробництво одиниці "Лимонаду" вимагає 2 од. "Води", 1 од. "Цукру" та 1 од. "Лимонного соку".
5. Виробництво одиниці "Фруктового соку" вимагає 2 од. "Фруктового пюре" та 1 од. "Води".

Використовуючи PuLP, створіть модель, яка визначає, скільки "Лимонаду" та "Фруктового соку" 
потрібно виробити для максимізації загальної кількості продуктів, дотримуючись обмежень на ресурси. 
Напишіть програму, код якої максимізує загальну кількість вироблених продуктів "Лимонад" та "Фруктовий сік", враховуючи обмеження на кількість ресурсів.

Критерії прийняття ДЗ:
Код виконується і повертає максимальну можливу загальну кількість вироблених продуктів "Лимонад" та "Фруктовий сік", враховуючи обмеження на кількість ресурсів.
'''

# import pulp

# # Ініціалізація моделі
# model = pulp.LpProblem('Maximize output', pulp.LpMaximize)

# # Визначення змінних
# A = pulp.LpVariable('A', lowBound=0, cat='Integer') # Кількість продукту А
# B = pulp.LpVariable('B', lowBound=0, cat='Integer') # Кількість продукту B

# # Функція цілі (Максимізація обсягу виробленої продукції)
# model += A + B, 'Output'

# '''
# 3. Обмеження ресурсів: 100 од. "Води", 50 од. "Цукру", 30 од. "Лимонного соку" та 40 од. "Фруктового пюре".
# 4. Виробництво одиниці "Лимонаду" вимагає 2 од. "Води", 1 од. "Цукру" та 1 од. "Лимонного соку".
# 5. Виробництво одиниці "Фруктового соку" вимагає 2 од. "Фруктового пюре" та 1 од. "Води".
# '''

# # Додавання обмежень
# model += 2 * A + 1 * B <=100 # Обмеження для "Води" (x)
# model += 1 * A <=50 # Обмеження для "Цукру"(y)  
# model += 1 * A <=30 # Обмеження для "Лимонного соку"(z)
# model += 2 * B <=40 # Обмеження для "Фруктового пюре"(s)

# # Розв'язання моделі
# model.solve()

# # Вивід результатів
# print("Виробляти продуктів А:", A.varValue)
# print("Виробляти продуктів B:", B.varValue)

# print('Максимізований обсяг виробленої продукції = ', pulp.value(model.objective))
# print('Статус розв\'язку задачі:', pulp.LpStatus[model.status])





### Завдання 2. Обчислення визначеного інтеграла

'''
Ваше друге завдання полягає в обчисленні значення інтеграла функції методом Монте-Карло.

 📖 Можете обрати функцію на власний розсуд.
Виконаємо побудову графіка.


✂️ Це можна запустити!

import matplotlib.pyplot as plt
import numpy as np

# Визначення функції та межі інтегрування
def f(x):
    return x ** 2

a = 0  # Нижня межа
b = 2  # Верхня межа

# Створення діапазону значень для x
x = np.linspace(-0.5, 2.5, 400)
y = f(x)

# Створення графіка
fig, ax = plt.subplots()

# Малювання функції
ax.plot(x, y, 'r', linewidth=2)

# Заповнення області під кривою
ix = np.linspace(a, b)
iy = f(ix)
ax.fill_between(ix, iy, color='gray', alpha=0.3)

# Налаштування графіка
ax.set_xlim([x[0], x[-1]])
ax.set_ylim([0, max(y) + 0.1])
ax.set_xlabel('x')
ax.set_ylabel('f(x)')

# Додавання меж інтегрування та назви графіка
ax.axvline(x=a, color='gray', linestyle='--')
ax.axvline(x=b, color='gray', linestyle='--')
ax.set_title('Графік інтегрування f(x) = x^2 від ' + str(a) + ' до ' + str(b))
plt.grid()
plt.show()



Отримаємо наступний результат.



1. Обчисліть значення інтеграла функції за допомогою методу Монте-Карло, інакше кажучи, знайдіть площу під цим графіком (сіра зона).

2. Перевірте правильність розрахунків, щоб підтвердити точність методу Монте-Карло, шляхом порівняння отриманого результату та аналітичних розрахунків або результату виконання функції quad. Зробіть висновки.

📖 Для перевірки обчислення визначеного інтеграла в Python ви можете використовувати бібліотеку SciPy, зокрема її функцію quad з підмодуля integrate. Спочатку необхідно визначити функцію, яку ви хочете інтегрувати, а потім використати quad для обчислення інтеграла на заданому інтервалі.


Приклад застосування функції quad



✂️ Це можна запустити!

import scipy.integrate as spi

# Визначте функцію, яку потрібно інтегрувати, наприклад, f(x) = x^2
def f(x):
    return x**2

# Визначте межі інтегрування, наприклад, від 0 до 1
a = 0  # нижня межа
b = 2  # верхня межа

# Обчислення інтеграла
result, error = spi.quad(f, a, b)

print("Інтеграл: ", result, error)



У цьому прикладі, функція quad повертає два значення: результат інтегрування та оцінку абсолютної помилки.



Виведення:

Інтеграл:  2.666666666666667 2.960594732333751e-14


Критерії прийняття ДЗ:

Програмно реалізовано алгоритм пошуку визначеного інтеграла за допомогою методу Монте-Карло. Код виконується та повертає значення інтеграла.
Виконано порівняльний аналіз результату, отриманого за допомогою алгоритму, з результатом, отриманим аналітично або за допомогою функції quad з підмодуля integrate бібліотеки SciPy.
Зроблено висновки щодо правильності розрахунків шляхом порівняння отриманих результатів і результатів, які дає функція quad або аналітичне обчислення інтеграла. Висновки оформлено у вигляді файлу readme.md домашнього завдання.
'''


# ======================
# Умови дано: Завдання 2
# ======================

# import matplotlib.pyplot as plt
# import numpy as np

# # Визначення функції та межі інтегрування
# def f(x):
#     return x ** 2

# a = 0  # Нижня межа
# b = 2  # Верхня межа

# # Створення діапазону значень для x
# x = np.linspace(-0.5, 2.5, 400)
# y = f(x)

# # Створення графіка
# fig, ax = plt.subplots()

# # Малювання функції
# ax.plot(x, y, 'r', linewidth=2)

# # Заповнення області під кривою
# ix = np.linspace(a, b)
# iy = f(ix)
# ax.fill_between(ix, iy, color='gray', alpha=0.3)

# # Налаштування графіка
# ax.set_xlim([x[0], x[-1]])
# ax.set_ylim([0, max(y) + 0.1])
# ax.set_xlabel('x')
# ax.set_ylabel('f(x)')

# # Додавання меж інтегрування та назви графіка
# ax.axvline(x=a, color='gray', linestyle='--')
# ax.axvline(x=b, color='gray', linestyle='--')
# ax.set_title('Графік інтегрування f(x) = x^2 від ' + str(a) + ' до ' + str(b))
# plt.grid()
# plt.show()



# Для перевірки обчислення визначеного інтеграла в Python ви можете використовувати бібліотеку SciPy, зокрема її функцію quad з підмодуля integrate. 
# Спочатку необхідно визначити функцію, яку ви хочете інтегрувати, а потім використати quad для обчислення інтеграла на заданому інтервалі.

# import numpy as np
# import scipy.integrate as spi

# # Визначте функцію, яку потрібно інтегрувати, наприклад, f(x) = x^2
# def f(x):
#     return x**2

# # Визначте межі інтегрування, наприклад, від 0 до 2
# a = 0  # нижня межа
# b = 2  # верхня межа

# # Обчислення інтеграла
# result, error = spi.quad(f, a, b)

# print("Інтеграл quad: ", result, error)




# ======================
# Розв'язання Завдання 2
# ======================

'''
Метод Монте-Карло обчислює визначений інтеграл як площу під кривою через випадкові точки.

Основна формула:
    b
    ∫ f(x) dx ≈ (b - a) * (1 / N) * ∑ f(x_i)
    a                               i=1 to N
де:
    x_i ∈ [a, b] — випадкові значення з рівномірного розподілу на відрізку [a, b],
    N — кількість точок (випробувань).

Ідея полягає в тому, щоб середнє значення f(x_i) на інтервалі [a, b]
помножити на ширину інтервалу (b - a) — це і дає наближення площі під кривою.


Алгоритм Монте-Карло (покроково) 
1. Задати N — кількість випадкових точок (наприклад, 100_000).
2. Згенерувати N випадкових значень x на інтервалі [a, b].
3. Обчислити значення f(x) для кожного x.
4. Знайти середнє значення f(x) (середню висоту).
5. Помножити на ширину інтервалу (b - a) → це й буде оцінка інтегралу.
'''

# --------------
#### Варіант 1. 
# --------------

## Кількість точок N = 100_000 (це добре, що багато точок - точність обчислення вище), 
## проте Кількість еспериментів тільки num_experiments = 1, що все ж зменшує точність приблизного обчислення інтеграла по алгоритму Монте-Карло

import numpy as np
import matplotlib.pyplot as plt
from scipy.integrate import quad

# Функція для інтегрування
def f(x):
    return x**2

# Межі інтегрування
a = 0
b = 2

# Кількість випадкових точок
N = 100_000

# Метод Монте-Карло
x_rand = np.random.uniform(a, b, N)  # Випадкові значення x ∈ [a, b] у кількості N значень (<class 'numpy.ndarray'>)
y_rand = f(x_rand)                   # Відповідні випадкові значення f(x)

mean_y = np.mean(y_rand)            # Середнє значення f(x)
monte_carlo_result = (b - a) * mean_y  # Площа ≈ ширина × середня висота 

# Аналітичний інтеграл через scipy
exact_result, error = quad(f, a, b)

# Виведення результатів
print('=== Варіант 1 ====\n')
print("  Інтеграл методом Монте-Карло  (кількість експериментів = 1):")
print(f"  Оцінка інтеграла методом Монте-Карло ≈ {monte_carlo_result}")

print("\n  Аналітичне значення інтеграла (через quad):")
print(f"  Інтеграл = {exact_result} (похибка = {error:.2e})")

print(f'\n  Абсолютна похибка Монте-Карло = Інтеграл Монте-Карло - Аналітичний інтеграл quad ≈ {abs(monte_carlo_result - exact_result):.5f}\n\n')




# --------------
#### Варіант 2. 
# --------------

## Кількість точок N = 100_000 (це добре, що багато точок - точність обчислення вище), 
## але вже Кількість еспериментів значна порівняно з Варіантом 1 -> num_experiments = 100, що покращує точність приблизного обчислення інтеграла по алгоритму Монте-Карло

import random
import numpy as np
import scipy.integrate as spi

# Визначення функції, яку потрібно інтегрувати, наприклад, f(x) = x^2
def f(x):
    return x ** 2

# Межі інтегрування
a = 0
b = 2

# Кількість експериментів (середнє за кілька запусків)
num_experiments = 100           # Кількість експериментів
points_per_experiment = 100_000 # кількість точок для кожного експерименту

def monte_carlo_integral(f, a, b, num_experiments, points_per_experiment):
    total = 0
    for _ in range(num_experiments):
        # Генеруємо випадкові точки x з інтервалу [a, b]
        xs = [random.uniform(a, b) for _ in range(points_per_experiment)]
        # Обчислюємо значення функції в цих точках
        values_y = [f(x) for x in xs]
        # Площа прямокутника: ширина інтервалу × середнє значення функції (висоти фігури)
        area = (b - a) * (sum(values_y) / len(values_y))
        total += area
    return total / num_experiments

# Обчислення інтегралу методом Монте-Карло
approx_integral = monte_carlo_integral(f, a, b, num_experiments, points_per_experiment)

# Аналітичне значення для порівняння
exact_integral, error = spi.quad(f, a, b)

# Виведення результатів
print('=== Варіант2 ====\n')

print(f"  Інтеграл методом Монте-Карло (кількість експериментів = {num_experiments}):")
print(f"  Оцінка інтеграла методом Монте-Карло ≈ {approx_integral}")

print(f"\n  Аналітичне значення інтеграла (через quad): {exact_integral}\n")

print(f'  Абсолютна похибка Монте-Карло = Інтеграл Монте-Карло - Аналітичний інтеграл quad ≈ {abs(approx_integral - exact_integral):.5f}')



# =============|
## Висновки    |
# =============|

'''
Для функції f(x) = x^2 на відрізку [0, 2]:

Точне значення визначеного інтеграла (Аналітичний інтеграл (через quad) ):
    2                 |2 
    ∫x² dx = [x³ / 3] | = 8 / 3 ≈ 2.6667 
    0                 |0

Метод Монте-Карло (наприклад, при запуску з кількістю точок N = 100_000 та кількостю експериментів 1) може дати оцінку інтеграла типу:
    Оцінка інтеграла ≈ 2.6681
    Абсолютна похибка = Інтеграл Метод Монте-Карло - Аналітичний інтеграл (через quad) = 2.6681 - 2.6667 ≈ 0.0014

Метод Монте-Карло (наприклад, при запуску з кількістю точок N = 100_000 та кількостю експериментів 100) може дати оцінку інтеграла типу:
    Оцінка інтеграла ≈ 2.6663
    Абсолютна похибка = Інтеграл Метод Монте-Карло - Аналітичний інтеграл (через quad) = 2.6663 - 2.6667 ≈ 0.0004

Це свідчить про високу точність методу Монте-Карло при великій кількості випадкових точок та великій кількості експериментів - зі збільшенням кількості точок та/або кількості експериментів -
точність обчислення визначеного інтегралу (площі під графіком) Методом Монте-Карло зростає (значно зменшується абсолютна похибка).
'''